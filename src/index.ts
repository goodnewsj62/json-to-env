/**
 * Public API for converting JSON (JSON5) configuration into `.env`-style text.
 */
export { jsonToEnv, type Options } from "./engine";
export type { ArrayJson, KeyCase, ObjectType } from "./types";
